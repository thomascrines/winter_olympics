<h2>Teams</h2>

<% @teams.each do |team| %>
 <p><%= team.name %> <%= team.code %> <%= team.golds %> <%= team.silvers %> <%= team.bronzes %> <%= team.points_total %></p>
 <img src="<%=team.flag_url %>">
 <br>
<% end %>